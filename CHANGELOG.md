# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.1] - 2026-09

### 修复

- **重复任务删不干净**：以前点 ✕ 一律只删当天，第二天任务又会被重新生成，看起来像没删掉。
  现在删除重复任务会给出两个明确选项——「只删今天」（只跳过这一次）和「今天及以后」
  （删掉这次及以后所有未完成的，并结束这个重复）。非重复任务保持原来的两步确认。
- 检查更新遇到网络抖动就失败：改为退避重试（手动检查 3 次、自动检查 2 次），
  界面会显示重试进度；同时识别 electron-updater 的报错包装，不再把整段英文原始报错丢给用户。

### 工程

- 发布流程在创建 Release 前会先清空该 tag 的旧资产，避免重发时把上一轮的旧安装包留在页面上
- 安卓签名配置新增自检步骤，失败原因以 `::error::` 注解暴露，不必翻日志

## [1.0.0] - 2026-09

首个公开版本。

### 桌面端（Windows / Electron）

- 每日任务管理：增删改查、勾选、双击改标题、拖拽排序
- 任务类型（工作 / 私人 / 未分类）、优先级、按类型筛选
- 到点提醒：精确到年月日时分，弹 Windows 系统通知
- 重复任务（每天 / 工作日 / 每周 / 每月），按天懒生成，可同步改动到未来实例
- 未完成自动顺延到今天，并标注来源日期
- 每日备忘，停手自动保存
- 月历总览、今日完成率、连续达成天数、最近 7 天柱状图
- 全量搜索（任务标题 / 备注 / 备忘）
- 四种主题：跟随系统 / 浅色 / 深色 / 星辰大海
- **系统托盘**：点 ✕ 继续后台运行，提醒不丢；开机自启静默驻留
- **自动更新**：启动后自动检查 GitHub Releases，后台下载，重启生效（设置里可关）
- 数据导出 / 导入 JSON

### 安卓端（Capacitor）

- 与桌面端功能一致的手机界面（单栏布局 + 底部操作栏 + 底部抽屉）
- **系统级本地通知**：交给 Android 排程，关掉应用也能提醒
- 触屏优化：抽屉式表单、16px 输入字号、两步删除确认

### 同步

- **局域网直连同步**：电脑当服务端，手机连同一 Wi-Fi 即可对齐
- 记录级「后写入者胜出」合并 + 删除墓碑（删除不会被合并回来）
- 手机打开应用 / 切回前台自动同步，顶栏 ⟳ 可手动同步
- 桌面端窗口标题栏显示同步入口；手机端设置页可填电脑地址

### 工程

- 桌面端零运行时依赖；安卓包约 3MB
- 数据层双实现（Node `fs` / 浏览器 `localStorage`）逻辑对齐，两端各有独立自测
- 数据层断言、两端端到端冒烟、同步 HTTP 端到端测试
- 冒烟测试含量化界面断言（文字截断、按钮被压成竖排等）

[1.0.0]: https://github.com/yexinghe9527/daily-memo/releases/tag/v1.0.0
